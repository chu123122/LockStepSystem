# 项目改造总结(STAR)

按层记录从"单球 demo"到"确定性帧同步框架"的改造。每点:背景 → 问题 → 解决 → 效果。

## 一、世界模型层

1. **背景**:物理逻辑散在 Unity 脚本里,服务器沾不上边。
2. **问题**:双端要跑同一套逻辑,逻辑必须脱离引擎。
3. **解决**:抽 LockStepCore:组件表、实体主表、物理系统、碰撞求解器。
4. **效果**:客户端服务器共用一份代码,开局同构、逐帧确定。

## 二、协议层

1. **背景**:协议从 C++ 直译,指令集定长 10 槽。
2. **问题**:空槽白传字节,一帧最多 10 条,没哈希没版本。
3. **解决**:指令集变长(计数+实条);新增 InitWorld 一次下发开局配置。
4. **效果**:带宽只随真实操作走;序列化统一成"读头+注册表分派",两端对称。

## 三、传输层

1. **背景**:裸 UDP 一丢包就卡死,TCP 又太慢。
2. **问题**:帧指令必须可靠,同时要低延迟。
3. **解决**:导入 kcp2k(KCP 的 C# 移植:ACK、快速重传、拥塞可关)。
4. **效果**:可靠传输材料就绪,适配层(M4)接上即用。

## 四、客户端表现层

1. **背景**:客户端脚本自己算物理,还引着已删的旧文件。
2. **问题**:逻辑必须全在确定性世界,Unity 只当壳。
3. **解决**:时钟驱动世界;壳按实体 Id 读位置插值;开局壳统一刷出。
4. **效果**:逻辑与表现彻底分离,场景摆件只剩编辑器用途。

## 五、工具链

1. **背景**:场景里摆好的球墙,服务器看不见。
2. **问题**:开局数据必须双端一致,且可反复编辑。
3. **解决**:挂标记组件,一键导出 world.json,服务器读后下发客户端。
4. **效果**:Unity 成了关卡编辑器,删光摆件也不影响运行时刷出。
